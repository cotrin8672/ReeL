# KiCad local state and automatic backups
.history/
*-backups/
*.kicad_*.lck
fp-info-cache

# ZMK build cache and generated firmware
/.cache/zmk/
/build/firmware/

# RMK build output and converted firmware
/target/
/*.hex
/*.uf2

# Local validation output
/tmp/

